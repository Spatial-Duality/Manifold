import SwiftUI
import ManifoldKit

// Legacy view — replaced by SourceCardRow in DashboardView.
// Kept as empty stub to avoid removing from Xcode project file.
struct SourceRow: View {
    var body: some View { EmptyView() }
}
